import styled from "styled-components";

export const CyverseTokenWrapper = styled.div`
    background: linear-gradient(to top,rgba(0,0,0,1), rgba(0,0,0,0.9));
    position: relative;
    z-index: 4;
    box-sizing: border-box;
    display: flex;
    justify-content: center;
    padding: 20px;
`